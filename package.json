{
  "name": "amex-monzo-sync",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "onboard:monzo": "node scripts/onboard.mjs monzo",
    "onboard:truelayer": "node scripts/onboard.mjs truelayer"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "^5.20260828.1"
  }
}
